# FastAPI Backend Configuration
# ================================

# API Base URL - where the backend API is accessible
#
# For DEVELOPMENT (npm run dev):
#   - Option 1: Use /api (vite proxy will forward to localhost:8000)
#   - Option 2: Use http://localhost:8000 (direct, requires CORS)
#
# For PRODUCTION (Docker):
#   - Use /api (nginx will proxy to backend service)
#
VITE_API_BASE_URL=/api

# Direct backend URL for development proxy (used by vite.config.ts)
# Only needed if VITE_API_BASE_URL=/api and running dev server
# VITE_BACKEND_URL=http://localhost:8000

# Optional: Add other environment variables as needed
# VITE_APP_NAME=Enterprise Nexus
# VITE_ENV=development
