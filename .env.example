# =================================================================
# Integration Environment Configuration
# =================================================================
# Copy this file to .env and update values for your environment
# DO NOT commit the actual .env file with real secrets!
# =================================================================

# -------------------------
# Database Configuration
# -------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=attendance_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Full database URL (constructed from above values)
# For Docker: use 'db' as host (service name)
# For local dev: use 'localhost' as host
DATABASE_URL=postgresql+psycopg2://postgres:your_secure_password_here@db:5432/attendance_db

# -------------------------
# JWT / Security
# -------------------------
# Generate a secure key: openssl rand -hex 32
SECRET_KEY=replace_this_with_a_strong_secret_key_use_openssl_rand_hex_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# -------------------------
# Application Settings
# -------------------------
# Options: development, staging, production
APP_ENV=development

# -------------------------
# pgAdmin (optional - only if using debug profile)
# -------------------------
PGADMIN_EMAIL=admin@local.dev
PGADMIN_PASSWORD=adminpass

# -------------------------
# Frontend Configuration
# -------------------------
# For production build, API calls go through nginx proxy at /api
# This is set at Docker build time via build args
# VITE_API_BASE_URL=/api

# -------------------------
# CORS (Backend)
# -------------------------
# Already configured in backend for http://localhost:3000
# Add additional origins here if needed (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
